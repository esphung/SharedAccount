name: Label PRs

on:
  pull_request_target:
    branches:
      - develop
    types: [opened, synchronize, reopened, unlabeled, labeled]

jobs:
  labeler:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v5
